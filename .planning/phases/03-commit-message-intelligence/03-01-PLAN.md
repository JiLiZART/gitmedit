---
phase: 03-commit-message-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/document.rs
  - src/renderer.rs
  - src/app.rs
autonomous: true
requirements: [COMMIT-01, COMMIT-02, COMMIT-03]
must_haves:
  truths:
    - "Subject line character count displays in real-time as user types"
    - "Counter is green for ≤50 chars, yellow for 51-72 chars, red for >72 chars"
    - "Blank line detection identifies missing blank line between subject and body"
    - "Blank line warning appears in status bar when no blank line present"
  artifacts:
    - path: "src/document.rs"
      provides: "first_line() and has_blank_line_after_subject() methods"
      min_lines: 20
    - path: "src/renderer.rs"
      provides: "render_status_bar() with counter and blank line detection"
      exports: ["render_status_bar"]
      min_lines: 15
    - path: "src/app.rs"
      provides: "Accessor methods for features added"
      exports: []
  key_links:
    - from: "src/renderer.rs"
      to: "src/document.rs"
      via: "calls document.first_line() and has_blank_line_after_subject()"
      pattern: "first_line|has_blank_line"
    - from: "src/renderer.rs"
      to: "src/app.rs"
      via: "reads app.document() for counter computation"
      pattern: "app.document()"
---

<objective>
Subject line counter (real-time character count with 50/72 color coding) and blank line detection with non-blocking warning in status bar.

Purpose: Guide users toward well-formed commit messages (50 char subject limit, 72 char soft limit, blank line separator).
Output: Subject line counter and blank line warning in status bar, methods in Document for extraction and detection.
</objective>

<execution_context>
@/Users/jilizart/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jilizart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-commit-message-intelligence/03-CONTEXT.md
@.planning/phases/03-commit-message-intelligence/03-RESEARCH.md
@.planning/phases/02-text-editing-comment-handling/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add first_line() and has_blank_line_after_subject() to Document</name>
  <files>src/document.rs</files>
  <read_first>src/document.rs (lines 1-150 to see existing impl structure, editable_lines() pattern)</read_first>
  <action>
Add two new methods to the Document impl block:

1. `pub fn first_line(&self) -> String` — Returns the first editable (Content) line as a String, empty string if no editable lines exist
   - Use the existing `editable_lines()` pattern: filter ContentLine::Content variants and collect into Vec
   - Return `.first().cloned().unwrap_or_default()`
   - This extracts the subject line (first editable line only)

2. `pub fn has_blank_line_after_subject(&self) -> bool` — Detects if a blank line exists between subject and body
   - Call `self.editable_lines()` to get editable lines only (comments/markers are filtered out)
   - If `editable_lines.len() < 2`, return `true` (single-line messages don't require blank line separator)
   - If `editable_lines.len() >= 2`, check line at index 1: `editable_lines[1].trim().is_empty()` returns true if blank, false if has text
   - A blank line is defined as: after .trim() produces an empty string (no visible chars, only whitespace or nothing)

Add unit tests in the tests module at bottom of document.rs:
- test_first_line_returns_subject: Parse "Subject\n\nBody\n" with comment char '#', verify first_line() == "Subject"
- test_first_line_empty_when_no_editable: Parse "# only comment\n", verify first_line() == ""
- test_has_blank_line_true_when_blank_present: Parse "Subject\n\nBody\n", verify has_blank_line_after_subject() == true
- test_has_blank_line_false_when_missing: Parse "Subject\nBody\n", verify has_blank_line_after_subject() == false
- test_has_blank_line_true_single_line: Parse "Subject\n", verify has_blank_line_after_subject() == true (single line doesn't require blank line)

No behavioral changes to existing methods; only additions.
  </action>
  <verify>
    <automated>cargo test --lib document::tests::test_first_line_returns_subject && cargo test --lib document::tests::test_has_blank_line</automated>
  </verify>
  <done>
    src/document.rs has two new methods (first_line and has_blank_line_after_subject). Five new unit tests passing. All existing Document tests still pass. Methods correctly extract subject line and detect blank line presence with .trim().is_empty() check.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update render_status_bar() to display character counter and blank line warning</name>
  <files>src/renderer.rs</files>
  <read_first>src/renderer.rs (entire file to see render_status_bar pattern and styling conventions)</read_first>
  <action>
Replace the existing `render_status_bar()` function with an updated version that displays:

1. Subject line character counter with color coding
   - Call `app.document().first_line()` to get subject line
   - Compute `char_count = first_line.chars().count()` (use .chars().count(), not .len(), for Unicode correctness)
   - Assign color based on count:
     * Green if count <= 50
     * Yellow if 51 <= count <= 72
     * Red if count >= 73
   - Format counter text: "Chars: {count}" (e.g., "Chars: 42")
   - Wrap in Span::styled with the computed color and bold modifier: `Span::styled(counter, Style::default().fg(color).bold())`

2. Blank line warning indicator
   - Call `app.document().has_blank_line_after_subject()` to check for blank line
   - If `false` (no blank line present), append warning: " [No blank line]" styled in yellow
   - If `true`, append empty span (no warning)
   - Implement as: `let blank_warning = if !has_blank { Span::styled(" [No blank line]", Style::default().fg(Color::Yellow)) } else { Span::raw("") };`

3. Status bar layout
   - Create Line with vec of spans: [counter_span, blank_warning_span, actions_span]
   - Keep existing save/cancel action text: " | ^S Save  Esc Cancel  ^H Help" (updated to include ^H for next plan)
   - Total status text should fit in one line; prioritize counter, then warning, then actions (actions may truncate on narrow terminals)

4. Styling
   - Maintain existing dark gray background + white text: `Style::default().bg(Color::DarkGray).fg(Color::White)`
   - Counter spans use specific colors (green/yellow/red) with bold; actions use default white

Render using Paragraph widget as before, unchanged otherwise.

Example status bar rendering:
- Subject "Fix the bug" (10 chars): "Chars: 10 [green] | ^S Save  Esc Cancel  ^H Help"
- Subject "This is a slightly longer subject line that exceeds 72 characters for demonstration purposes" (92 chars): "Chars: 92 [red] [No blank line] [yellow] | ^S Save  Esc Cancel  ^H Help"

No changes to render_content() or other methods.
  </action>
  <verify>
    <automated>cargo test --lib renderer::tests || cargo test --lib 2>&1 | grep -E "test result|FAILED"</automated>
  </verify>
  <done>
    render_status_bar() updated to display "Chars: {count}" with green/yellow/red color coding. Blank line warning " [No blank line]" shows in yellow when absent. Status bar includes "^S Save  Esc Cancel  ^H Help" actions. Background dark gray, text white. Counter updates smoothly on keystroke. No visual artifacts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for counter colors and blank line rendering</name>
  <files>src/renderer.rs</files>
  <read_first>src/renderer.rs (especially the render_status_bar function and test module structure if it exists)</read_first>
  <action>
Add unit tests to the renderer.rs tests module (create one if doesn't exist) to verify counter color logic:

1. test_counter_color_green_at_50:
   - Mock a Document with first_line() returning a 50-char string
   - Verify that the color assigned is Color::Green

2. test_counter_color_yellow_at_60:
   - Mock a Document with first_line() returning a 60-char string
   - Verify that the color assigned is Color::Yellow

3. test_counter_color_red_at_80:
   - Mock a Document with first_line() returning an 80-char string
   - Verify that the color assigned is Color::Red

4. test_blank_line_warning_not_shown_when_present:
   - Mock a Document with has_blank_line_after_subject() returning true
   - Verify that the warning span is empty (no "[No blank line]" text)

5. test_blank_line_warning_shown_when_missing:
   - Mock a Document with has_blank_line_after_subject() returning false
   - Verify that the warning span contains " [No blank line]" text styled in yellow

These tests do not require full frame rendering; they test the logic for color assignment and warning text selection only. Use simple string/color assertions or mock the Document/App as needed.

Note: Tests may use App::new() with test content strings directly, or extract the color-assignment logic into a helper function for easier testing.
  </action>
  <verify>
    <automated>cargo test --lib renderer::tests 2>&1 | grep -E "test_counter_color|test_blank_line_warning" | head -10</automated>
  </verify>
  <done>
    Five unit tests added to renderer.rs. test_counter_color_green_at_50, test_counter_color_yellow_at_60, test_counter_color_red_at_80 verify color transitions. test_blank_line_warning_not_shown_when_present and test_blank_line_warning_shown_when_missing verify warning display logic. All 5 tests passing. `cargo test --lib renderer::tests` exits 0.
  </done>
</task>

</tasks>

<verification>
Run full test suite to verify no regressions and all new features working:
```
cargo test --lib
```
All tests should pass. Then perform manual verification:
1. Open gitmedit with a commit message file: `gitmedit /tmp/test_commit_msg`
2. Type a short subject line (e.g., "Fix bug") — verify counter shows green
3. Type more to exceed 50 chars — verify counter turns yellow
4. Type more to exceed 72 chars — verify counter turns red
5. Type a subject line without blank line after it, then add body text — verify "[No blank line]" warning appears in yellow
6. Add a blank line — verify warning disappears
7. Verify status bar shows: "Chars: {count} [color] | ^S Save  Esc Cancel  ^H Help"

No crashes, no terminal corruption.
</verification>

<success_criteria>
- COMMIT-01: Subject line character counter displays in real-time (verified via manual test above)
- COMMIT-02: Counter shows correct colors (green ≤50, yellow 51-72, red >72)
- COMMIT-03: Blank line enforcement indicator appears/disappears correctly
- Character count uses `.chars().count()` for Unicode safety
- Blank line detection uses `.trim().is_empty()` to ignore trailing whitespace
- Status bar text fits within terminal width (no truncation of counter)
- All unit and integration tests pass
- Next plan (help overlay) can proceed with App state extended as needed
</success_criteria>

<output>
After completion, create `.planning/phases/03-commit-message-intelligence/03-01-SUMMARY.md`
</output>
