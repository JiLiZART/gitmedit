---
phase: 05-installation-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - LICENSE
  - README.md
autonomous: true
requirements:
  - INSTALL-01
  - INSTALL-02
  - INSTALL-03
  - INSTALL-04
must_haves:
  truths:
    - "Cargo.toml has all crates.io-required metadata fields (description, license)"
    - "LICENSE file exists with MIT license text"
    - "README.md exists with installation and git configuration instructions"
    - "cargo publish --dry-run passes without errors"
  artifacts:
    - path: "Cargo.toml"
      provides: "crates.io-ready package metadata"
      contains: "description ="
    - path: "LICENSE"
      provides: "MIT license text"
      contains: "MIT License"
    - path: "README.md"
      provides: "Installation and configuration documentation"
      contains: "cargo install gitmedit"
  key_links:
    - from: "Cargo.toml"
      to: "README.md"
      via: "readme field"
      pattern: 'readme = "README.md"'
    - from: "Cargo.toml"
      to: "LICENSE"
      via: "license field"
      pattern: 'license = "MIT OR Apache-2.0"'
---

<objective>
Add crates.io-required metadata to Cargo.toml, create LICENSE and README.md files so the package is ready for publication.

Purpose: Without these fields, `cargo publish` fails. This plan makes gitmedit installable via `cargo install gitmedit` from crates.io and via `cargo install --path .` from the repo.
Output: Cargo.toml with full metadata, LICENSE file, README.md with install/config instructions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-installation-distribution/05-CONTEXT.md
@.planning/phases/05-installation-distribution/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add crates.io metadata to Cargo.toml and create LICENSE file</name>
  <files>Cargo.toml, LICENSE</files>
  <read_first>
    - Cargo.toml (current state -- must preserve existing dependencies and profile settings)
    - .planning/phases/05-installation-distribution/05-RESEARCH.md (recommended Cargo.toml fields)
  </read_first>
  <action>
Update Cargo.toml [package] section to add the following fields after the existing `edition = "2024"` line:

```toml
description = "Fast, distraction-free git editor with nano-style shortcuts and context-aware modes for commits, merges, and interactive rebase"
license = "MIT OR Apache-2.0"
repository = "https://github.com/jilizart/gitmedit"
readme = "README.md"
keywords = ["git", "editor", "tui", "commit", "rebase"]
categories = ["command-line-utilities", "development-tools"]
```

Do NOT add a `homepage` field (no dedicated website exists; setting it to the same URL as repository is redundant per crates.io docs).

Do NOT change any existing fields (name, version, edition), dependencies, dev-dependencies, or profile.release sections.

Create a LICENSE file at the project root containing the standard MIT License text with:
- Copyright year: 2026
- Copyright holder: gitmedit contributors

The MIT license text is the standard OSI-approved template starting with "MIT License" followed by "Copyright (c) 2026 gitmedit contributors" and the full permission/warranty paragraphs.

Note: The `license` field uses `"MIT OR Apache-2.0"` (dual license, standard Rust convention). The LICENSE file contains MIT text. Users who prefer Apache-2.0 can reference the SPDX identifier. This is the most common approach in the Rust ecosystem for dual-licensed crates.
  </action>
  <verify>
    <automated>cd /Users/jilizart/Projects/Github/gitmedit && grep -q 'description = ' Cargo.toml && grep -q 'license = "MIT OR Apache-2.0"' Cargo.toml && grep -q 'repository = ' Cargo.toml && grep -q 'readme = "README.md"' Cargo.toml && grep -q 'keywords = ' Cargo.toml && grep -q 'categories = ' Cargo.toml && test -f LICENSE && grep -q 'MIT License' LICENSE && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml contains `description = "Fast, distraction-free git editor`
    - Cargo.toml contains `license = "MIT OR Apache-2.0"`
    - Cargo.toml contains `repository = "https://github.com/jilizart/gitmedit"`
    - Cargo.toml contains `readme = "README.md"`
    - Cargo.toml contains `keywords = ["git", "editor", "tui", "commit", "rebase"]`
    - Cargo.toml contains `categories = ["command-line-utilities", "development-tools"]`
    - Cargo.toml does NOT contain `homepage =`
    - Cargo.toml still contains `[dependencies]`, `[dev-dependencies]`, `[profile.release]` sections unchanged
    - LICENSE file exists at project root
    - LICENSE contains "MIT License"
    - LICENSE contains "2026"
    - `cargo check` passes (Cargo.toml is valid TOML)
  </acceptance_criteria>
  <done>Cargo.toml has all 6 metadata fields for crates.io. LICENSE file exists with MIT text. Existing dependencies and profile unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md with installation and git configuration instructions</name>
  <files>README.md</files>
  <read_first>
    - Cargo.toml (to reference correct package name and version)
    - .planning/phases/05-installation-distribution/05-CONTEXT.md (locked decisions on what to document)
    - .planning/phases/05-installation-distribution/05-RESEARCH.md (git config commands, install commands)
  </read_first>
  <action>
Create README.md at the project root with the following structure and content:

**Title:** `# gitmedit`

**Tagline paragraph:** "A fast, distraction-free git editor with nano-style shortcuts. Understands git context -- commits, merges, interactive rebase, and squash operations -- without bloat."

**## Features section** with bullet list:
- Context-aware modes: automatically detects COMMIT_EDITMSG, MERGE_MSG, git-rebase-todo, and SQUASH_MSG
- Subject line intelligence: real-time character counter with color coding (green <=50, yellow 51-72, red >72)
- Interactive rebase table: structured display with action cycling (pick/squash/fixup/drop)
- Squash mode: read-only commit log with editable combined message
- Nano-style shortcuts: Ctrl+S save, Esc cancel, Ctrl+H help
- No alternate screen: prior terminal output remains visible (like nano)
- Fast startup: under 100ms

**## Installation section:**

```
### From crates.io

```sh
cargo install gitmedit
```

### From source

```sh
git clone https://github.com/jilizart/gitmedit.git
cd gitmedit
cargo install --path .
```
```

**## Configuration section:**

```
### Set as default git editor

```sh
# For commit messages
git config --global core.editor gitmedit

# For interactive rebase
git config --global sequence.editor gitmedit
```

### Verify configuration

```sh
git config --global --get core.editor    # should print: gitmedit
git config --global --get sequence.editor # should print: gitmedit
```
```

**## Usage section** with brief descriptions:
- Commit: `git commit` opens gitmedit with subject line counter and blank line detection
- Rebase: `git rebase -i HEAD~3` opens structured rebase table
- Squash: During rebase squash operations, shows commit log as read-only context

**## Keyboard shortcuts section** as a markdown table:
| Shortcut | Action |
|----------|--------|
| Ctrl+S | Save and exit |
| Esc | Cancel and exit |
| Ctrl+H | Toggle help overlay |
| Tab | Cycle rebase action (rebase mode) |

**## Platform support section:**
- Linux, macOS, and Windows
- Uses crossterm for cross-platform terminal handling

**## License section:**
"Licensed under MIT OR Apache-2.0"

Do NOT include:
- `gitmedit --setup` instructions (deferred to v2)
- Shell completion instructions (deferred to v2)
- Homebrew/apt installation (deferred to v2)
- Version checking or auto-update (deferred to v2+)
  </action>
  <verify>
    <automated>cd /Users/jilizart/Projects/Github/gitmedit && test -f README.md && grep -q '# gitmedit' README.md && grep -q 'cargo install gitmedit' README.md && grep -q 'core.editor' README.md && grep -q 'sequence.editor' README.md && grep -q '## Installation' README.md && grep -q '## Configuration' README.md && cargo publish --dry-run 2>&1 | head -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists at project root
    - README.md contains `# gitmedit` as first heading
    - README.md contains `## Features`
    - README.md contains `## Installation`
    - README.md contains `cargo install gitmedit`
    - README.md contains `cargo install --path .`
    - README.md contains `## Configuration`
    - README.md contains `git config --global core.editor gitmedit`
    - README.md contains `git config --global sequence.editor gitmedit`
    - README.md contains `## Usage`
    - README.md contains `## Keyboard shortcuts` or `## Keyboard Shortcuts`
    - README.md does NOT contain `--setup`
    - README.md does NOT contain `shell completion` or `completions`
    - README.md does NOT contain `homebrew` or `apt`
    - `cargo publish --dry-run` does not report missing readme
    - `cargo package --list` output includes README.md
  </acceptance_criteria>
  <done>README.md exists with installation, configuration, usage, and shortcuts sections. No deferred v2 features mentioned. cargo publish --dry-run finds the readme.</done>
</task>

</tasks>

<verification>
1. `cargo check` passes (Cargo.toml valid)
2. `cargo publish --dry-run` passes (all required metadata present, README found, LICENSE found)
3. `cargo package --list` includes README.md and LICENSE
4. No deferred features (--setup, shell completions, homebrew) appear in any created file
</verification>

<success_criteria>
- Cargo.toml has description, license, repository, readme, keywords, categories fields
- LICENSE file exists with MIT text
- README.md exists with install + git config instructions
- `cargo publish --dry-run` passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-installation-distribution/05-01-SUMMARY.md`
</output>
